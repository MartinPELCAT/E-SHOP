import React, { Component } from "react";
import { Switch, Route, RouteComponentProps } from "react-router";

export default class UserRoutes extends Component<RouteComponentProps> {
  render() {
    return (
      <Switch>
        <Route>/hdjksadhkj</Route>
      </Switch>
    );
  }
}
